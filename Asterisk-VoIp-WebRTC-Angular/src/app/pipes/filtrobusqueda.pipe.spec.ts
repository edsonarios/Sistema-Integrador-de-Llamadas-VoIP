import { FiltrobusquedaPipe } from './filtrobusqueda.pipe';

describe('FiltrobusquedaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrobusquedaPipe();
    expect(pipe).toBeTruthy();
  });
});
