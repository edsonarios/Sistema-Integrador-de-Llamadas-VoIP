import { FilteroperPipe } from './filteroper.pipe';

describe('FilteroperPipe', () => {
  it('create an instance', () => {
    const pipe = new FilteroperPipe();
    expect(pipe).toBeTruthy();
  });
});
